package com.greyway.fees_up

import io.flutter.embedding.android.FlutterFragmentActivity

/**
 * Extends FlutterFragmentActivity to support plugins that require a FragmentActivity
 * context (e.g., local_auth for biometric authentication).
 */
class MainActivity : FlutterFragmentActivity()
